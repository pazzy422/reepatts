# Minimal foundry.toml for reepatts (Reentrancy Pattern Scanner)
# This is a bytecode scanner, not a Solidity project, but cast requires
# a config to find the project root.
[profile.default]
src = "scripts"
out = "out"
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
